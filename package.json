{
  "name": "sampla",
  "version": "0.1.43",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "npm test",
    "test": "node --test tests/*.test.mjs",
    "stems:smoke": "node scripts/stem-smoke.mjs"
  },
  "dependencies": {
    "onnxruntime-web": "1.30.0"
  }
}
